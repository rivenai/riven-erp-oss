<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Portal users can only see their own CRM opportunities -->
        <record id="rule_portal_crm_own" model="ir.rule">
            <field name="name">Portal: CRM - Own Transaction Only</field>
            <field name="model_id" ref="crm.model_crm_lead"/>
            <field name="domain_force">[('partner_id.user_ids', 'in', [user.id])]</field>
            <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <!-- Portal users can only see their own Sign requests -->
        <record id="rule_portal_sign_own" model="ir.rule">
            <field name="name">Portal: Sign Requests - Own Only</field>
            <field name="model_id" ref="sign.model_sign_request"/>
            <field name="domain_force">[('request_item_ids.partner_id.user_ids', 'in', [user.id])]</field>
            <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>
    </data>
</odoo>
